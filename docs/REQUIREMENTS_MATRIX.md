# Deep-research requirements matrix

This matrix maps the report’s concrete requirements to current evidence. It is
intentionally separate from `IMPLEMENTED.md` (evidence already present) and
`IMPLEMENTATION_TODO.md` (work still open).

This matrix covers both `deep-research-report (1).md` and the NYC hourly-market
focus in `deep-research-report (2).md`.

| Report requirement | Current evidence | Verification | Status / blocker |
| --- | --- | --- | --- |
| Settlement station/window/bucket semantics | `settlement_semantics.py`, PHX/LV schedule and settled-label artifacts | `python3 -m unittest scripts/weather/test_p0_semantics.py` | Partial: licensed/event-specific settlement replay is unavailable |
| Bitemporal point-in-time joins | `pit.py`, `asof_forecasts.py`, quality gates | `python3 scripts/weather/data_quality_gates.py --output data/weather_research/reports/data_quality_gates.json` | Partial: ASOS dissemination times and final-label publication times are incomplete |
| Historical station observations | GHCN labels, complete 2020–2026 GHCNh yearly/normalized observations, ASOS backfill, nearby ASOS, raw METAR, HOMR station history | `python3 scripts/weather/audit_artifact_inventory.py --output data/weather_research/reports/artifact_inventory.json`; GHCNh and `city_asos_archive` gates in `data_quality_gates.json`; `homr/manifest.json` | Implemented within public-data limits; 99 IEM ASOS raw monthly archives now have URL/hash/receipt manifests; publication-time provenance remains conservative |
| NBM/HRRR/GFS/GEFS/ECMWF guidance | 94 NOAA manifest rows, 958 decoded points across 66 source files, 3 explicit unavailable NBM f000 HTTP-404 rejections, GEFS member/features tables, ECMWF deterministic (25) and ensemble (75) decoded points | forecast-point, forecast-rejection, live-GEFS manifest/member-denominator, plus ECMWF archive/point/ensemble gates in `data_quality_gates.json` | Partial: current HRRR/NBM/GEFS vectors pass configured field completeness; live GEFS archive is integrity-checked (33 receipts, observed 30 members each), but archives are not historical OOS skill evidence |
| Calibrated distributions and bucket probabilities | climatology/persistence/postprocessor modules and PHX/LV bucket artifact | `python3 scripts/weather/evaluate_bucket_probabilities.py ...` | Diagnostic only; no settlement-source promotion |
| Intraday nowcasting | state backfill, nowcast forecasts, solar, GOES join/diagnostic | nowcast and GOES tests | Diagnostic only; GOES overlap has no eligible labels |
| Current AWC METAR observations | `collect_awc_metar.py` raw JSON plus normalized CSV with valid and receipt timestamps; `run_awc_poll.py` bounded/continuous cadence runner | AWC provenance check in `data_quality_gates.py`; `test_collect_awc_metar.py`; `test_run_awc_poll.py` | Collector and one-minute runner implemented for current NYC, Los Angeles, and Austin snapshots (197 normalized rows after the 2026-09-13 refresh); station-distance metadata and authenticated long-running deployment remain operational tasks |
| LAMP / GLMP short-range guidance | `collect_lamp.py` archives the latest NOAA LAMP ASCII cycle; `parse_lamp.py` emits station/field/valid-time rows with raw provenance | `test_collect_lamp.py`; `test_parse_lamp.py`; `lamp_archive` and `lamp_station_forecasts` checks | LAMP station parser and integrity gate implemented (19,812 current rows); field availability, GLMP multi-city decoding, and historical PIT skill study remain open |
| NYC hourly contract / settlement rules | `collect_kalshi_series.py` archives the complete `KXTEMPNYCH` series response plus cursor-crawled event/market payloads with receipt/hash provenance | `test_collect_kalshi_series.py`; normalized CSVs, raw JSONs, SQLite hourly tables | Implemented for the current public archive (3,806 events / 15,883 markets); latest manifest records a completed 20-page cursor crawl and event strike-date bounds (2026-03-11 to 2026-09-10); TWC continuous outcome-source retrieval remains data-source dependent |
| Contract terms provenance | Referenced Kalshi product-certification and terms PDFs are archived with receipt time, content type, byte count, and SHA-256 | `collect_contract_terms.py`; `data/weather_research/kalshi_hourly/terms/manifest.json` | Implemented: both referenced PDFs archived; terms are reconciled with the supplied listing-coordinate registry while preserving the TWC approximate-coordinate caveat |
| NYC hourly contract normalization | `parse_kalshi_hourly_contracts.py` emits target UTC/local text, threshold, operator, bucket bounds, settlement source, explicit station identity, rules hash, and raw provenance per market | `test_parse_kalshi_hourly_contracts.py`; normalization quality gate; SQLite `kalshi_hourly_contracts` | Implemented for all 15,883 archived markets; all map to KNYC via the supplied listing-coordinate registry; source-version reconciliation remains explicit |
| LA/Austin hourly contract normalization | `parse_kalshi_hourly_contracts.py` and `kalshi_hourly/kxtemp*_terms/contracts.csv` | `audit_intraday_city_readiness.py`; `test_parse_kalshi_hourly_contracts.py` | Implemented for LA (15,418 markets) and Austin (15,364 markets) with exact target/source/station/rules provenance; historical quote availability is not proven |
| Exact portal-to-contract labels | `join_kalshi_twc_labels.py` joins only exact target UTC observations, applies whole-degree contract bucket semantics, records source availability/receipt, and preserves Kalshi's settled binary result when continuous source temperature is unavailable | `test_join_kalshi_twc_labels.py`; `kalshi_hourly_twc_labels` quality gate; SQLite table | Implemented for all 15,883 currently crawled markets after July–September portal backfill; unmatched continuous values remain NULL/blank and are never replaced with ASOS or interpolation. The portal remains a public evidence surface, not a guaranteed licensed final-TWC export |
| TWC/Kalshi public source observations | `collect_twc_kalshi.py` archives the linked portal's KNYC/KLAX/KAUS hourly and daily JSON payloads with raw hashes, receipt clocks, and normalized rows | `test_collect_twc_kalshi.py`; `twc_kalshi_portal_archive` quality gate; SQLite tables | Implemented for July 6–September 13, 2026 (13,506 hourly rows, 1,110 daily rows after the latest refresh), with exact joins covering the current market archive; source-transition/final-licensed-feed reconciliation remains open |
| GLMP 2-m temperature guidance | `collect_glmp_temperature.py` discovers/archives the latest `fcsts_t` CONUS GRIB2 object; `decode_glmp_temperature.py` extracts PIT-gated city points | `test_collect_glmp_temperature.py`, `test_decode_glmp_temperature.py`; archive and point provenance gates | Raw and point extraction implemented for NYC, LA, and Austin (50 rows) in the pinned ecCodes environment; historical PIT skill study remains open |
| Conservative candle execution backtest | `simulate_candle_execution.py` separates prior closed signal candles from next-period conservative/base/optimistic fills, charges fees/slippage, exposes YES/NO edge diagnostics, and reports scenario-level payoff, drawdown, Sharpe/Sortino, turnover, and fill-rate metrics | `test_simulate_candle_execution.py`; deterministic scenario rows and summary | Implemented as a research simulator; empirical slippage and maker queue validation require future L2 capture |
| Robustness / stress diagnostics | `robustness_scenarios.py` applies fail-closed feed delays, late-NWP delays, stale-observation outages, calibration shocks, threshold proximity, and market-stress subsets | `test_robustness_scenarios.py`; JSON scenario output with Brier/log-loss and rejection counts | Framework implemented; aligned NYC prediction/outcome rows are still required for substantive go/no-go results |
| Binary probability calibration | `probability_calibration.py` provides separate-partition Platt and isotonic fitting/application | `test_probability_calibration.py`; bounded Platt parameters and monotonicity tests | Implemented as a calibration component; must be fit only on held-out calibration folds and validated on settlement-aligned outcomes |
| Slow climate covariates | CPC ONI raw/normalized data and SQLite `cpc_oni` | CPC quality gate | Implemented as a regime covariate, not a daily label |
| ERA5 / ERA5-Land covariates | `collect_era5.py` credential-aware CDS request contract | fail-closed credential test; raw request manifest when `CDS_API_KEY` is supplied | Scaffold implemented; actual retrieval remains blocked until Copernicus credentials are provided |
| RTMA analysis covariates | four raw objects, 30 point features, SQLite `rtma_point_features` | RTMA provenance/range/audit-clock gate | Implemented for the retained sample |
| Market metadata/trades/candles | PHX/LV and auxiliary city archives, plus historical NYC REST candles/trades | `collect_historical_nyc_quotes.py`; `historical_nyc_quote_archive` quality gate | Implemented as a bounded NYC archive with 5,004 metadata rows, 2,052 candles, and 24,491 trades; LA/Austin quote probes add 30 candles each (393 and 41 trades). Duplicate market-period rows remain visible in raw CSV and are deduplicated in SQLite. REST aggregates do not provide historical full-depth L2 or prove pre-decision quote availability |
| Three-city intraday PIT dataset and probability baseline | `build_intraday_edge_dataset.py`, `generate_intraday_baseline_predictions.py`, `run_intraday_edge_pipeline.py` | `reports/intraday_edge_pipeline_status.json`; `test_build_nyc_edge_dataset.py`; `test_generate_edge_baseline_predictions.py` | Implemented fail-closed for NYC/LA/Austin; current datasets admit 0 rows because quote/weather receipts are not demonstrably pre-decision, so 0 probabilities/manifests are emitted |
| Kalshi Weather Index (hourly-line input) | `weather_index/manifest.json` plus NYC raw minute series; LA/Austin HTTP failures retained | `weather_index` check in `data_quality_gates.json` | Partial: public endpoint currently accepts NYC only; unsupported city identifiers are not aliased or fabricated |
| Historical full-depth L2 capture | `collect_orderbook.py` scaffold and dry-run configuration | `python3 scripts/weather/collect_orderbook.py --dry-run` | Blocked: credentials and prospective capture required; no fabricated books |
| Market-efficiency, execution, sizing, and live validation | `simulate_market_efficiency.py` research-only candle/settlement analyzer with fee/slippage reserves, YES/NO edge, trigger threshold, and payoff summary; no order submission | unit test plus generated `phx_lv_market_efficiency.csv` | Partial: analyzer is implemented, but retained probabilities and candles have zero overlapping decision timestamps; the prior RSA-authenticated WS epoch had 36 populated daily books/174 deltas, while the newest epoch has empty snapshots, so replay/gates remain fail-closed and hourly KXTEMP books/continuous reconciliation remain open |
| Offline L2 execution replay | `replay_orderbook.py` snapshot/delta state machine and fee-aware level fills | `test_replay_orderbook.py` | Implemented as a research simulator; empirical fill-tolerance validation awaits captured L2 streams |
| Capped risk sizing | `risk_sizing.py` fractional-Kelly calculator with fees, event loss cap, allocation cap, and moving-block bootstrap drawdown/tail diagnostic | `test_risk_sizing.py`; `risk_sizing.py --returns ...` | Implemented as recommendation-only research output; portfolio validation still requires aligned settled returns/L2 |
| Multiple-testing-aware strategy selection | Centered moving-block max-statistic reality-check-style comparison across candidate returns | `multiple_testing.py`; `test_multiple_testing.py` | Implemented diagnostic; it is not claimed as formal SPA/Deflated-Sharpe inference |
| Microstructure features | `orderbook_features.py` spread/depth/imbalance/time-to-close extraction | `test_orderbook_features.py` | Implemented offline; model training awaits captured L2 data |
| Immutable prediction manifests | `prediction_manifest.py` writes sorted-key immutable JSON per prediction carrying `decision_ts`, `market_ticker`/`target_time_utc` (contract join), feature/model/calibrator versions, source run/observation IDs, `rules_hash`, `code_commit` (7–40 hex or `HEAD`), and a finite `[0,1]` `prediction`; it resolves IDs against optional source/observation indexes and verifies `rules_hash`/`target_time_utc` against a contract CSV; `data_quality_gates.py` `prediction_manifest` check recursively audits `predictions/**/*.json` (including `model_version=<v>/`), rederives content hashes, rejects duplicates, and optionally joins `contracts.csv` plus `source_index.csv`/`observation_index.csv`; `audit_artifact_inventory.py` counts recursively and pairs with `sqlite_prediction_manifests` | `test_prediction_manifest.py`; `prediction_manifest` gate in `data_quality_gates.json` | Implemented as a fail-closed writer/audit; the gate is intentionally red until a model actually emits manifests |
| Operational readiness / kill switch | `operational_readiness.py` evaluates feed/model freshness, probability consistency, clock drift, unresolved order-book gaps, settlement-rule verification, calibration drift, and drawdown against explicit thresholds | `test_operational_readiness.py`; JSON CLI | Implemented as a deterministic research/paper-trading control; it fails closed and never submits orders |
| Paper-trading ledger | `paper_trading_ledger.py` converts conservative execution rows into append-only would-have-order and simulated-fill records keyed to a prediction manifest | `test_paper_trading_ledger.py`; JSONL ledger CLI | Implemented research-only; real ledger population awaits valid aligned manifests and quote/L2 data |
| Paper-trading runner | `run_paper_trading.py` repeatedly ingests refreshed execution rows, validates the immutable manifest, and deduplicates ledger records | `test_run_paper_trading.py`; `--iterations 0` continuous mode | Implemented transport-agnostic runner; it never submits orders and requires upstream execution/quote telemetry |
| Deployment gate | `deployment_gate.py` evaluates every report-2 real-money prerequisite and fails closed on absent evidence | `test_deployment_gate.py`; JSON CLI | Implemented as a non-authorizing evidence gate; current data cannot satisfy it |
| Forecast variable completeness | Required HRRR/NBM/LAMP/GEFS fields are audited individually instead of inferring completeness from row counts | `audit_forecast_completeness.py`; `test_audit_forecast_completeness.py`; `reports/forecast_completeness.json` | HRRR, NBM, and GEFS configured vectors are complete in the current archive (958 decoded points; 3 unavailable NBM f000 requests are explicit in `forecasts/rejections.csv`; refreshed GEFS features include 2,544 rows and 30 members per current city/model cycle); LAMP remains incomplete because the retained aviation feed exposes only CIG/OBV/VIS, so the gate stays data-gated |
| Settlement target resolution | Event-level settlement source, target timestamp, rules hash, and station-resolution evidence are audited independently | `audit_settlement_targets.py`; `settlement_locations.json`; `settlement_target_resolution` quality gate; `reports/settlement_target_audit.json` | Implemented for KXTEMPNYCH using the supplied KNYC/Central Park IEM/NWSCLI coordinate (40.77900,-73.96925); source-specific display differences remain documented |
| Settlement source transition | Effective TWC-to-Synoptic era is parsed from the captured series notice and checked against every normalized target timestamp | `audit_settlement_source_transition.py`; `settlement_source_transition` quality gate; `reports/settlement_source_transition.json` | Implemented: all 15,883 retained rows are pre-transition TWC rows, with zero source-era mismatches; no post-transition Synoptic rows are present in the public archive |
| Live schedules | `live_schedule.py` emits the report-2 versioned cadence manifest for market, weather, model, pipeline, risk, settlement, QC, recalibration, and research jobs | `test_live_schedule.py`; JSON CLI | Implemented as a declarative schedule; it does not launch jobs or imply live authorization |
| Trading-state schemas | SQLite exposes report-2 `decision_features`, `prediction_manifests`, `paper_orders`, `paper_fills`, `paper_settlements`, `orderbook_events_live`, and `market_trade_events` tables | `load_sqlite.py`; artifact inventory; SQLite schema inspection | Implemented as empty-safe query-layer schemas; rows require valid manifests or prospective telemetry and are never synthesized |
| Coherent predictive distribution API | `GaussianTemperatureDistribution` exposes `cdf`, `prob_above`, `quantile`, `mean_f` and `stddev_f` semantics; bucket pricing reuses the same monotone CDF | `test_p0_semantics.py`; `normal_bucket_probabilities` partition tests | Implemented as a transparent Gaussian baseline; calibration and settlement-aligned model promotion remain open |
| Report-2 canonical source schemas | SQLite exposes named `settlement_temperature`, `surface_observation`, and `model_runs` tables alongside the trading-state layer | `load_sqlite.py`; `build_model_runs.py`; artifact inventory; SQLite schema inspection | Implemented: 118 model-run provenance rows materialized with init/ingest clocks and explicit `published_ts_method=unobserved`; settlement/surface tables remain empty-safe where authoritative publication metadata is unavailable |
| Report-2 model ladder eligibility | M0–M13 candidate inputs are enumerated before training and cannot be called complete when required artifacts are empty | `model_ladder.py`; `test_model_ladder.py`; `reports/model_ladder.json` | Implemented as a fail-closed eligibility manifest; current M0–M5 inputs have rows, while M6–M13 await non-late PIT decision features; M12/M13 remain optional challengers pending M11 improvement |
| Rolling-origin partitioning | Complete target events remain in one chronological train, calibration, or test partition with optional embargo | `training_readiness.py`; `test_training_readiness.py`; `reports/training_readiness.json` | Implemented as a fail-closed planner; current artifact is `data_missing` until PIT rows arrive |

## Current verification baseline

Run from the repository root:

```bash
python3 -m unittest discover scripts/weather -p 'test_*.py'
python3 -m py_compile scripts/weather/*.py
python3 scripts/weather/validate.py
python3 scripts/weather/data_quality_gates.py --output data/weather_research/reports/data_quality_gates.json
python3 scripts/weather/audit_artifact_inventory.py --output data/weather_research/reports/artifact_inventory.json
python3 scripts/weather/run_intraday_edge_pipeline.py
git diff --check
```
