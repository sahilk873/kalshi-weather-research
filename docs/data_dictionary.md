# Data dictionary and storage contract

Large backfills should be partitioned Parquet by `city/year/month` while raw
HTTP, CSV, METAR, GRIB, and WebSocket payloads remain immutable alongside
their normalized outputs. Live JSONL can later be ingested into PostgreSQL or
TimescaleDB; do not discard raw source messages.

| Dataset | Key / essential fields | Time semantics |
| --- | --- | --- |
| `stations` | station ID, ICAO, city, coordinates, elevation, timezone, role | static reference |
| `ghcn/labels_daily.csv` | station/date, `tmax_f`, `tmin_f`, flags, source, `label_available_ts` | final labels only; availability is conservative synthetic, not source publication |
| `iem/asos_parsed.csv` | station, `valid_utc`, `raw_metar`, temperature/dewpoint, wind, visibility, pressure, clouds/weather/precipitation, precise/6h/24h remark groups | `valid_utc` is observation event time; raw METAR is preserved |
| `city_asos/manifest.json` | city/station, requested UTC interval, IEM URL, raw path, SHA-256, receipt timestamp and receipt method | 99 cached IEM monthly archives; cached-file mtime is explicitly labeled as an integrity receipt, not network dissemination time |
| `awc/metar.csv` + raw snapshot | current AWC METAR station observations, separate valid/receipt times, raw METAR text, source URL, raw snapshot path, and payload hash | low-latency NYC support feed; raw payload and manifest are preserved, not a historical substitute |
| `lamp/` | latest NOAA NOMADS LAMP ASCII bulletin plus retrieval/hash manifest; `station_forecasts.csv` normalized station/field/valid-time rows | parser preserves numeric and categorical fields with raw hash/receipt provenance; current bulletin may omit requested fields, so model eligibility remains field-gated |
| `glmp/` | latest NOAA NOMADS GLMP CONUS 2-m temperature forecast GRIB2 plus snapshot manifest | raw guidance archive; decode with a pinned ecCodes/wgrib2 environment before modeling |
| `glmp/point_features.csv` | GLMP city point temperature, initialization/valid/lead/receipt times, grid coordinates, raw path/hash, message index | decoded with pinned ecCodes in `/tmp/kalshi-eccodes`; 50 current rows cover NYC, LA, and Austin; no skill claim without historical runs |
| `ghcnh/observations_2020_2026.csv` / SQLite `ghcnh_observations` | GHCNh station, UTC timestamp, coordinates/elevation, temperature/dewpoint °F, quality/report fields, raw path/hash, source receipt time | normalized from complete 2020–2026 yearly PSV files; manifest retrieval time (or immutable file mtime when an integrity-only manifest omits it) supplies receipt provenance; quality gates reject invalid/future receipts; rows are auxiliary observation evidence, not official settlement labels; Austin registry station is USW00013904 |
| `ghcnh/daily_extrema_2020_2026.csv` / SQLite `ghcnh_daily_extrema` | station, local calendar date/timezone, sampled observation count, max/min temperatures and UTC times, contributing raw paths/hashes | derived from GHCNh hourly rows using station-local civil dates; quality-gated to the five registered stations (Austin `USW00013904`); diagnostic extrema, not official CLI settlement labels |
| `ghcnh/settlement_window_extrema_2020_2026.csv` / SQLite `settlement_window_extrema` | PHX/LV outcome date, UTC local-standard-time window, sampled count, high/low values and timestamps, raw hashes | 4,884 PHX/LV station-day diagnostics using `settlement_standard_time_window`; `settlement_window_extrema` quality gate validates scope/order/ranges/provenance; supports DST-safe oracle review but is not a settlement-source replay |
| `reports/ghcnh_vs_ghcn_daily.json` | overlap count plus city/type mean delta, MAE, and maximum absolute delta | read-only cross-source diagnostic; station identity and daily-window differences remain reviewable rather than silently reconciled; `ghcnh_daily_comparison` quality gate validates the report |
| `reports/phx_lv_market_efficiency.csv` | PIT bucket probability, candle ask at/before decision, gross edge, and settled outcome | research-only join; empty output is valid when quote and forecast windows do not overlap; no order placement or fill claim |
| `replay_orderbook.py` output JSON | message count, sequence gaps/resets, last sequence, quarantine status, requested/filled quantity, level fills, fees, total cost | offline replay only; unresolved sequence gaps quarantine fills until a verified resnapshot, and no live order is sent |
| `reconcile_orderbook.py` output JSON | WebSocket-versus-REST side/price/quantity comparison and explicit mismatch list | deterministic read-only reconciliation; no book is repaired or promoted when levels disagree; no captured REST comparison exists yet |
| `reports/orderbook_reconciliation_*.json` | latest reconciliation result, level counts, and mismatch count | quality gate requires a passing report before executable depth can be considered; currently absent |
| `risk_sizing.py` output JSON | Kelly fraction, edge, capped allocation, contract count, and maximum loss; event allocator shares one cap across buckets | recommendation-only sizing diagnostic; requires explicit bankroll and risk caps; does not submit orders |
| `orderbook_features.py` output mapping/CSV | best bid/ask, spread, top depth, imbalance, sequence gaps, minutes to close | derived from replayed L2 state or its CLI JSONL-to-CSV path; missing sides are surfaced as NaN rather than imputed |
| `weather_index/manifest.json` + raw city JSON | Kalshi public Weather Index response, config version, minute-series count, retrieval time, hash, and explicit HTTP failures | contract-specific hourly-market input; NYC currently returns 1,430 points while LA/Austin probes are retained as 400 failures, never substituted with aliases; `weather_index` quality gate validates structure and hashes |
| `weather_index/nyc_timeseries.csv` | normalized NYC index timestamp, Fahrenheit value, contributor count, status, config version, receipt time, raw path/hash | derived only from the archived JSON; 1,430 rows with millisecond timestamps converted to UTC; not a daily NWS settlement label |
| `homr/*.json` / `homr/manifest.json` | NCEI HOMR station history, identifiers, locations, observing metadata, raw response hash and retrieval timestamp | `date=all` station-history evidence; metadata only, never a weather label or forecast input without explicit point-in-time treatment |
| `homr/station_history.csv` / SQLite `homr_station_history` | normalized identifier, relocation, location, platform, update, and remark records keyed by station/history facts | derived only from archived HOMR JSON; metadata joins must respect each record's begin/end dates |
| `ecmwf/manifest.json` + `ecmwf/raw/*.grib2` | ECMWF IFS cycle, parameter/steps, retrieval time, raw path, byte size, SHA-256, decode status | current open-data forecast provenance; raw file is not a decoded PIT feature until a GRIB decoder is available |
| `ecmwf/point_features.csv` / SQLite `ecmwf_point_features` | ECMWF IFS point temperature, city, init/valid/receipt times, lead, grid point, raw path/hash | 25 decoded rows from the current 00Z run; usable as current guidance, not historical OOS skill evidence |
| `era5/manifest.json` (when credentials are available) | CDS ERA5 request specification, retrieval timestamp, and raw NetCDF path | credential-gated reanalysis covariate; never an operational point-in-time forecast |
| `reports/data_quality_gates.json` (`homr_archive`) | HOMR manifest/path/hash/JSON/station/timestamp checks | all five archived station histories must pass before metadata joins are used |
| `reports/data_quality_gates.json` (`ecmwf_point_features`) | decoded ECMWF path/hash, timestamp, scope, unit/range, and duplicate checks | all decoded point rows must pass before use as guidance |
| `ecmwf/ensemble_points.csv` / SQLite `ecmwf_ensemble_points` | ECMWF IFS member ID, point temperature, init/valid/receipt times, lead, grid point, raw path/hash | 75 rows from three perturbation members at 0/3/6/9/12h; current-run guidance only |
| `ecmwf/ensemble_summaries.csv` | city/lead member count, mean, population spread, p10/p50/p90, threshold probabilities, source hash | 25 distributions computed only from the three available members |
| `reports/ghcnh_vs_in_scope_asos.json`, `reports/ghcnh_vs_city_asos.json` | exact station/time overlap, match counts, temperature delta MAE/mean/max, unmatched counts | read-only cross-source consistency diagnostics; GHCNh and IEM dissemination timestamps are not inferred |
| `nearby_asos/nearby_asos_parsed.csv` | city, station, `valid_utc`, `local_date`, tmpf/dwpf/relh, wind (drct/sknt/gust), p01i, alti/mslp, vsby, sky layers, wxcodes, ice/snow/peak/feel follow IEM 39-column naming, `remark_*` (precise temps, 1/2/4h extrema), `raw_metar` | six deliberately selected nearby stations (PHX: SDL/CHD/FFZ, LAS: HND/VGT/LSV); `valid_utc` uses canonical `Z`; deduplicated on (station, `valid_utc`) because IEM re-delivers rows |
| `iem/asos_daily.csv` | station/local date, sampled extrema and times, coverage count | a convenience aggregate; never label it official |
| `solar/solar_features.csv` | city/date, sunrise/sunset UTC/local, day length, declination | deterministic local-calendar values; calculate instantaneous angles at a feature timestamp |
| `kalshi/events.csv` | event/series/city/high-low/outcome date, settlement source URL, source rules | API retrieval snapshot; settlement source must remain joined to event |
| `kalshi/series_catalog.json` | complete `/series` response, request URL, retrieval timestamp, and discovered climate-temperature series metadata | collector discovery snapshot; the seeded catalog is a fallback, and newly discovered series require independent station/timezone/settlement verification |
| `kalshi/markets.csv` | ticker, bucket bounds, status/result, open/close/settlement timestamps, quotes/OI/volume | API-snapshot quotes are not historical book states |
| `kalshi/trades.csv` | trade ID, ticker, created timestamp, price, count, taker sides | event time is Kalshi `created_time` |
| `kalshi/candlesticks_*.csv` | ticker, interval, period end, OHLC, bid/ask, OI, volume | period end must be no later than decision time |
| `kalshi/ws/*.jsonl` | `received_ts`, `received_ts_ms`, raw message | ingestion/receive time plus exchange message timestamps where supplied; current RSA-authenticated capture preserves dollar-priced snapshot levels and deltas without order submission |
| `reports/orderbook_capture_*.json` | raw-file SHA-256, message counts, ticker coverage, receipt bounds, connection-epoch selection, sequence gaps/resets, populated snapshot count | immutable audit of a captured WebSocket file; the newest epoch is evaluated for executable continuity, while older epochs remain retained evidence; `full_depth_evidence` requires populated levels and valid JSON; replay quarantines fills after unresolved gaps until a verified resnapshot |
| `reports/daily_source_transition_*.json` | supplied Aug 14 NWS→TWC notice, expected CLINYC/CLILAX/CLIAUS product, target date, rule-text validation, input hash, and explicit rejection reasons | active post-transition daily probe: 36/36 verified TWC rows; historical rows without event-level source metadata are rejected rather than inferred |
| `reports/live_daily_diagnostic_predictions_*.csv` | active daily ticker, target/threshold semantics, registered CLINYC/CLILAX/CLIAUS rule match, pre-quote forecast receipt, quote receipt, raw GEFS mean/stddev, Gaussian contract probability, best bid/ask, spread, displayed depth proxy, YES and NO gross/net edge under parameterized fee/slippage scenarios | 72 current rows passed station-rule and receipt ordering on 2026-09-13; defaults are explicitly zero-cost diagnostics unless CLI assumptions are supplied; uncalibrated and unlabeled, never a live/paper prediction manifest or realized P&L |
| `reports/live_price_comparison_summary.json` | aggregate positive candidate counts and edge ranges from the live diagnostic | 72 rows / 27 positive candidates per scenario; settled rows, realized edge, and P&L are intentionally null |
| `forecasts/manifest.csv` + `forecasts/raw/{model}` | model, city/grid point, initialization time, valid time, lead, request URL, retrieval time, SHA-256; immutable filtered GRIB2 | require both init and valid time; never choose a later run; decode only when a GRIB decoder is installed |
| `derived_intraday_state.csv` | timestamp, observation extrema, reconstructed extrema, 5/15/30/60m deltas, acceleration, dewpoint/wind/cloud changes, solar clocks | built strictly from rows public by timestamp; final labels are not read. Deduplicated before groupby (IEM can re-deliver identical rows); rows without a present temperature are kept with empty per-time deltas |
| `nws_cli/daily_climate_cli.csv` | city, climate date, climatological high/low, spatial source, publication timestamp, raw filename | product revisions preserved across rows; join climate dates honoring publication time to avoid lookahead |
| `kalshi/contract_outcomes.csv` | outcome date, city, temp type, bucket bounds + label, status, settled yes/no, settlement source+URL | derived view of events × markets; settled label strictly separate from any pre-settlement feature |
| `kalshi_weather.sqlite` | normalized query tables: stations catalogs (GHCN-adjacent + ASOS), observations, labels, intraday state, solar, Kalshi events/markets/prices/contract outcomes, nearby observations, NWS CLI daily, forecast manifest, historical quote metadata/candles/trades, live top-of-book quote snapshots, and report-2 trading-state schemas | `INSERT OR REPLACE` on natural keys; live quotes key on `(market_ticker, received_ts)`; model manifests retain `available_time_utc` and `available_time_method` (conservative ingest when publication is unobserved); raw files remain authoritative; rebuild with `load_sqlite.py` |
| `city_observations`, `city_daily_labels`, `city_nws_cli` (SQLite) | auxiliary NYC/LA/Austin observations, GHCN labels, and CLI revisions | loaded from `city_asos/`, `ghcn_city/`, and `city_nws_cli/`; these are forecast-research tables and do not certify Kalshi settlement |
| `city_kalshi_markets`, `city_kalshi_trades`, `city_kalshi_candles` (SQLite) | auxiliary city hourly market metadata, public trades, and aggregate candles | loaded from `kalshi_historical_city/`; public historical cutoff and no L2-depth claim apply |
| `forecasts/raw/{hrrr,nbm}` + `forecasts/manifest.csv` | filtered GRIB2, model, city, initialization/valid time, lead, request URL, hash, ingest timestamp | raw NOAA archive evidence; a decoder is required before extracting variables; missing/404 NBM runs remain missing rather than synthesized |
| `forecasts/model_forecasts_points.csv` / `model_forecast_points` | nearest-grid-point HRRR/NBM/GFS variables, units, grid coordinates, issue/valid/receipt times, raw path/hash, GRIB message index | decoded in an isolated ecCodes environment; current archive has 958 rows across 66 source files; multiple messages for one variable/lead are retained rather than collapsed |
| `forecasts/rejections.csv` / `forecast_rejections` | model/city/init/valid/lead/request URL/retrieval time/reason for unavailable or failed NOMADS requests | explicit quarantine ledger; current archive contains 3 NBM f000 HTTP-404 rows and none are used as forecast features |
| `gefs/gefs_members.parquet` | forecast/valid time, city coordinates/timezone, model, member ID, variable, value | long-form member rows; raw Open-Meteo JSON remains under `gefs/raw/`; run time is retrieval time when initialization is not exposed; observed smoke-test responses returned 30 members |
| `gefs/gefs_features.parquet` | ensemble mean/spread, p10/p25/median/p75/p90, member count, threshold probabilities | computed from `temperature_2m`; `prob_ge_K` is members with temperature >= K divided by available members |
| `daily_ensemble_forecasts.csv` | research target ID, city, high/low type, standard-time target date, model/run, member count, mean/stddev, issue/receipt/decision timestamps | each member's hourly temperature is reduced to a daily maximum or minimum inside the city's fixed local-standard-time window before distribution summaries; generated IDs are not Kalshi tickers |
| `climatology_forecasts.csv` | research target ID, city, high/low type, target date, mean/stddev, training row count, issue/receipt/decision timestamps | monthly same-city baseline built only from prior labels available by decision time; target-day and future labels are excluded |
| `persistence_forecasts.csv` | same forecast contract as climatology, with latest-prior mean, trailing spread, training row count, and PIT timestamps | latest prior label is selected only when its availability timestamp is no later than the decision; no prior history is rejected rather than imputed |
| `reports/city_daily_market_schedule.csv` / `city_daily_market_labels.csv` | event-level city, high/low type, market opening decision time, target local date, lead, and PIT-gated proxy label | derived from public daily market metadata; GHCN labels are explicitly proxy diagnostics because Weather Company settlement rules are not replayed |
| `reports/city_baseline_skill_aggregate.csv` | sample-weighted MAE/CRPS by model, city, type, lead, eligible count, and rejection totals | aggregates `evaluate_forecast_skill.py` monthly output; zero rejections in the current 4,143-row diagnostic run does not establish NWP or trading performance |
| `reports/city_*_walk_forward.csv` | fold ID, train/test date ranges, model, city/type/lead, test count, MAE, CRPS | 365-day train / 30-day test chronological diagnostics; current outputs contain 48 folds and 3,781 test predictions with zero evaluator rejections |
| `reports/city_nowcast_forecasts.csv` / `city_nowcast_labels.csv` | observation-only nowcast distributions, running-extrema feature timestamp, residual training count, and PIT-gated proxy labels | 41,084 forecasts from ASOS state; no NWP or Weather Company settlement claim |
| `reports/city_nowcast_walk_forward.csv` | 90-day train / 30-day test fold metrics for the observation-only nowcast | 26,896 test predictions, zero evaluator rejections; diagnostic prerequisite for NWP-controlled comparison |
| `reports/city_blend_walk_forward.csv` | fold-level PIT convex climatology/persistence blend weights and MAE/CRPS | 48 non-overlapping folds / 3,781 predictions; weighted MAE/CRPS 5.697/4.064; proxy-label diagnostic, not an NWP or trading result |
| `reports/phx_lv_bucket_probabilities.csv` | PHX/LV event-market probabilities, bucket bounds, standard-time window, and forecast provenance | 1,680 Gaussian climatology bucket rows; accepted event partitions sum to 1.0; research diagnostic only |
| `reports/phx_lv_settled_labels.csv` | finalized PHX/LV event-to-settled-market labels with GHCN availability timestamp | 264 events with one settled-YES market; missing/latest and multiple-YES cases are rejected explicitly |
| `scripts/weather/build_settled_market_labels.py` | deterministic settled-label adapter | joins only normalized `settled_yes` outcomes to labels available for PIT evaluation; never infers an outcome from prices |
| `reports/city_sequence_windows.csv` | fixed-length ASOS feature sequence, feature names, window timestamps, city/type, proxy label and availability | 8,914 PIT-gated windows with a 120-minute maximum inter-observation gap; data contract for a future TCN/LSTM/TFT study, not a trained model |
| `reports/goes_cloud_availability.json` | satellite, ABI product, requested day/hour, listing URL, object keys/count, probe status and retrieval time | availability evidence; imagery is retained separately and any derived feature must carry its source hash |
| `reports/goes_cloud_station_features.csv` | station, GOES source hash/time, projection pixel, 3×3 C13 infrared brightness-temperature mean, DQF clear fraction | three bounded public G18 MCMIPC scans for PHX/KLAS; temporal feature artifact only, not yet a scored nowcast input |
| `reports/goes_cloud_asos_state_20260911.csv` | ASOS observation/extrema plus latest prior GOES scan, cloud age, C13 feature, DQF, and source hash | six-point PHX/KLAS point-in-time overlap; rows with future or stale scans are excluded |
| `reports/goes_incremental_rows_20260911.csv` / `_summary.csv` | per-observation baseline and cloud-adjusted absolute errors, plus grouped MAE | research-only diagnostic; current run has no eligible rows because proxy labels end before the GOES observations |
| `reports/phx_lv_intraday_state_backfill.csv` | full PHX/LV running-extrema, delta, wind/dewpoint/cloud, and solar state with feature-as-of timestamps | 88,165 rows generated from the merged ASOS backfill; separate from the active trailing state and no final labels are joined |
| `rtma/manifest.csv`, `rtma/raw/*.grb2`, `rtma_point_features.csv`, SQLite `rtma_point_features` | RTMA 2.5-km hourly analysis metadata plus nearest-grid-point temperature/dewpoint/wind/cloud/visibility values | four archived objects (three `rqirtma` plus one variable-bearing 00Z analysis); point features retain source hash and are research-only |
| `cpc/oni.ascii.txt`, `cpc/oni.csv`, SQLite `cpc_oni` | CPC seasonal Niño 3.4 mean/anomaly with source URL and retrieval timestamp | 919 rows; slow regime covariate only, never a point-in-time daily label |
| `reports/data_quality_gates.json` | machine-readable provenance, timestamp, duplicate, availability, and RTMA/CPC value-range checks | decoded forecast-point hashes are checked against their raw GRIB files; unavailable historical payloads remain explicitly red; the overall gate also remains red while the strict NYC dataset, manifests, and active-market probe are empty |
| `kalshi_hourly/series.json`, `events.csv`, `markets.csv`, `raw/*.json`, SQLite `kalshi_hourly_events`/`kalshi_hourly_markets` | immutable `KXTEMPNYCH` series metadata and nested event/market contract records, including settlement sources, timestamps, receipt/hash provenance | current public archive: 3,806 events and 15,883 markets; continuous TWC outcome readings are not synthesized |
| `kalshi_hourly/kxtemp*_terms/contracts.csv` and `raw/*.json` | event-crawled normalized terms for `KXTEMPNYCH`, `KXTEMPLAXH`, and `KXTEMPAUSH`, including exact target times, source-era text, station mapping, rules hashes, receipt clocks, and raw hashes | NYC 3,806 events/15,883 markets; LA 1,524/15,418; Austin 1,522/15,364; terms are complete for the bounded public crawl but do not contain historical bid/ask availability |
| `kalshi_city/active_probe/latest.json` plus `raw/active_*.json` | multi-city open-market counts, ticker list, receipt timestamp, and SHA-256 of the combined public response | latest probe: 72 daily NYC/Los Angeles/Austin contracts open and zero hourly KXTEMP contracts; daily and hourly scopes remain separate |
| `kalshi_city/active_probe/quotes_*.csv` | receipt-stamped public top-of-book bid/ask prices and displayed sizes for every open city market, linked to the immutable probe payload | prospective quote evidence only; it is not a reconstructed historical order book or maker-fill record |
| `kalshi_hourly/contracts.csv`, SQLite `kalshi_hourly_contracts` | parsed target UTC/local text, exact ticker threshold, comparison operator, bucket bounds, event-specific settlement source URL/name, station identity/method, event-derived rules hash, and raw payload provenance | 15,883 normalized contracts; event payload sources override series metadata; KNYC mapping uses the supplied Central Park registry with source/display-coordinate caveats |
| `kalshi_hourly/twc_labels.csv`, SQLite `kalshi_hourly_twc_labels` | exact target-time portal temperature, whole-degree-bucket source-derived binary result, Kalshi result, observation/receipt clocks, availability timestamp, and raw source provenance | 15,883 contract rows and 15,883 exact source-time matches; all retained labels remain subject to the independent PIT availability gate, which currently rejects them as late |
| `twc_kalshi/hourly.csv`, `daily.csv`, `raw/*.json`, `manifest.json`, SQLite `twc_kalshi_hourly`/`twc_kalshi_daily` | public Weather Company/Kalshi portal KNYC/KLAX/KAUS observations and daily climate reports, with status, valid time, receipt time, source URL payload hash and raw path | current capture: 12,566 target-station hourly rows and 1,036 daily rows across Jul 6–Sep 13, 2026 (30 retained snapshots); manifest marks availability as `receipt_upper_bound` with `source_publication_time_observed=false`, so this is source evidence, not an inferred replacement for event labels |
| `reports/artifact_inventory.json` | generated UTC timestamp, CSV/SQLite row counts, and `sqlite_count_mismatches` | reproducible current-state inventory after a true SQLite rebuild; mismatch list must be empty; regenerate with `audit_artifact_inventory.py` |
| `reports/city_climatology_{bias,quantile,emos}_walk_forward.csv` | fold-specific postprocessor model/version and test metrics | fits only labels available by the later fold as-of clock; current outputs are diagnostics and do not promote any postprocessor to a production candidate |
| `reports/city_climatology_boosted_walk_forward.csv` | optional HistGradientBoosting p10/p50/p90 fold metrics | scikit-learn is optional; current 3,781-test diagnostic is retained for audit and fails the CRPS promotion gate |
| `station_bias_forecasts.csv` | original forecast fields plus corrected mean, bias, training-row count, model version | residual correction is fitted only from labels available by each training decision; fallback groups are explicit and spread is not silently recalibrated |
| `emos_forecasts.csv` | original forecast fields plus corrected mean/stddev, affine coefficients, spread scale, training-row count, model version | Gaussian EMOS-style correction; training is point-in-time gated and must be evaluated on untouched outer folds |
| `reports/nowcast_skill_summary.csv` | city, local hour, eligible state rows, observed-extrema MAE against final labels, deterministic rejection counts | diagnostic only; each feature timestamp must precede label availability, and no NWP comparison is implied |
| `reports/bucket_skill_summary.csv` / `bucket_skill_rejections.csv` | model identity, city/type, event counts, multiclass Brier, log loss, probability partition sum, deterministic rejection reasons | labels are settlement-market tickers and must become available strictly after decision; no market prices are used |
| `validation_folds.json` / `reliability_bins.csv` | fold train/test dates, embargo, probability-bin counts, mean probabilities, empirical event rates | folds are event-date based so buckets from one event cannot cross train/test; calibration summaries are descriptive only |
| risk bootstrap JSON | observation/simulation counts, block size, terminal-return quantiles, max-drawdown tail, loss probability | generated only from explicitly supplied settled-return CSV; no synthetic returns are used in project reports |
| multiple-testing JSON | candidate strategies, observed studentized means, selected candidate, block-bootstrap max-statistic p-value | centered under the no-positive-edge null; diagnostic only until aligned out-of-fold returns are supplied |
| `quantile_forecasts.csv` | original forecast fields plus p10/p50/p90, converted mean/stddev, training-row count, model version | empirical residual quantiles trained only on labels available before each training decision; requires outer-fold validation |
| `blend_forecasts.csv` | blended mean/stddev, model identity/version, member count, member weights, decision/issue/receipt timestamps | convex mixture of eligible constituent distributions; mixture uncertainty includes member disagreement and requires outer-fold evaluation |
| `gefs/historical_raw/` | historical, previous-runs, and single-runs JSON, manifest, predictions, calibration | point-in-time forecast provenance; actual/error joins require separately timestamped observations |
| `bucket_probabilities.csv` | event/market IDs, local-standard-time target window, decision/issue/receipt timestamps, model identity, continuous distribution parameters, bucket bounds and probability | generated only from a forecast whose issue **and receipt** times are no later than `decision_time_utc`; a missing or unusable issue/receipt timestamp is rejected (fails closed) and the event's bucket partition is rejected on gaps **or** overlaps; all buckets for a forecast-event form one probability partition |
| `skill_summary.csv` / `skill_rejections.csv` | model/version, city, high/low type, lead hours, target month, MAE/CRPS, eligibility and deterministic rejection counts | `evaluate_forecast_skill.py` requires forecast issue/receipt ≤ decision and label availability strictly after decision; it never invents climatology/persistence/NWP forecasts |
| `simulate_candle_execution.py` output CSV | market, decision candle, next execution candle, scenario, fair probability, fill price, fee, gross edge, settlement result, and net payoff | conservative/base/optimistic research-only execution scenarios; no queue-aware maker-fill claim |
| `robustness_scenarios.py` JSON output | scenario name, input/filtered/scored/rejected counts, Brier score, and log loss | deterministic stress diagnostics; missing timestamps/fields fail closed and no outcome is synthesized |
| `probability_calibration.py` model JSON | calibration method, parameters/monotone blocks, training-row count, and fallback status | fit on a distinct calibration partition; no settlement outcome is invented |
| `city_focus_audit.json` | registered city, timezone, station/CLI/GHCN identity, hourly and daily Kalshi series, row counts, duplicate keys, timestamp counts, and scope warning | read-only audit for the auxiliary NYC/Los Angeles/Austin forecast layer; Weather Company event rules are not asserted as verified daily labels |
| `city_asos/asos_parsed_canonical.csv` + `asos_duplicate_resolution.csv` | one canonical observation per `(station, valid_utc)` plus retained/superseded row hashes, scores, and reasons | raw ASOS remains untouched; canonicalization is deterministic and auditable, selecting the most populated re-delivery |
| `reports/city_daily_market_audit.json` | daily auxiliary series, city/type, market/event counts, resolved/unresolved rows, event-date range, settlement-source names/URLs, and rules-reproduced flag | catalog evidence only; the rules-reproduced flag is intentionally false because Weather Company payloads are not available for independent replay |
| `predictions/**/*.json` | one report-2 prediction manifest per file: market identity, `decision_ts`, `target_time_utc`, feature/model/calibrator versions, source/observation IDs, `rules_hash`, `code_commit`, and bounded probability `prediction` | written by `prediction_manifest.py` as deterministic content-addressed sorted-key JSON; supplied contract/provenance indexes and PIT clocks are verified, and recursive quality-gate auditing fails closed on drift, duplicates, or unresolved metadata |
| `operational_readiness.py` JSON output | readiness boolean, deterministic reason codes/details, and active thresholds for feed/model age, probability consistency, clock drift, order-book gaps, rules verification, calibration error, and daily drawdown | research/paper-trading kill-switch decision only; no order submission or capital authorization |
| Paper-trading JSONL ledger | append-only `prediction`/`would_have_order`/`simulated_fill`/`settlement` records with manifest reference, market, decision clock, side, price, fee, scenario, and payoff | duplicate IDs and missing manifest references fail closed; records are hypothetical and never sent to Kalshi |
| `reports/live_schedule.json` | versioned job names, cadence seconds, class, and continuous flag for report-2 operations | declarative cadence contract; scheduler integration and external job execution remain separate |
| SQLite trading-state tables | `decision_features`, `prediction_manifests`, `paper_orders`, `paper_fills`, `paper_settlements`, `orderbook_events_live`, and `market_trade_events` | report-2 query-layer schemas; currently empty because no aligned prediction/L2 telemetry is fabricated |
| `GaussianTemperatureDistribution` | reusable monotone temperature CDF, exceedance probability, inverse quantile, mean, and standard deviation | transparent continuous baseline used to derive mutually exclusive bucket probabilities; not a promoted calibrated model |
| SQLite canonical source schemas | `settlement_temperature`, `surface_observation`, and `model_runs` tables matching report-2 source provenance fields | query-layer contracts are present; rows are not populated from substitute or fabricated sources |
| `reports/data_quality_gates.json` | as-of clock, duplicate-key counts, timestamp missing/invalid/future counts, forecast-manifest hash/path checks, historical-forecast null-payload/quarantine checks, live GEFS raw-hash/member-count checks, settlement-target resolution, and per-city row counts | read-only cross-source integrity scan; current overall `pass` is false because the strict NYC dataset, prediction manifests, baseline/evaluation outputs, and active-market probe are empty; KNYC station resolution itself passes from the supplied registry |
| `reports/decision_features.csv` / `decision_feature_rejections.csv` | PIT feature rows or explicit market-level rejection reasons; payload includes temporal slopes/extrema, station gradients, solar geometry when contract coordinates exist, forecast distribution statistics, and residuals when source fields exist | generated by `build_decision_features.py`; current run has zero eligible rows and distinguishes missing sources from late observations/forecasts; no late source is silently joined |
| `feature_store.py` output CSV / rejection CSV | Long-form `(city, contract, target_time_utc, decision_time_utc)` feature vintages with provider/name, event/issue/valid/retrieval clocks, revision, effective `available_at`, availability method, latency scenario, value, and raw provenance | exact availability and observed receipt clocks are admitted directly; estimated historical availability requires an explicit reference timestamp and caller-supplied optimistic/base/conservative/highly-conservative latency policy; the latest eligible revision is selected independently per source and feature |
| `reports/model_runs.csv` / SQLite `model_runs` | model-run ID, provider/model/version, initialization, publication method, ingestion time, grid resolution, raw manifest URI | generated by `build_model_runs.py`; 118 rows materialized from NOAA/Open-Meteo/GLMP manifests; publication time remains explicitly `unobserved` when providers do not expose it |
| `reports/model_ladder.json` | M0–M13 candidate model IDs, required input artifact, row count, input-presence status, trained/OOS/promotion flags, and explicit missing/deferred reason | generated by `model_ladder.py`; input presence is never a performance result; M4 counts only temperature-bearing LAMP/GLMP rows (aviation-only CIG/VIS/OBV rows are excluded); decision-feature-dependent models remain `data_missing` because the PIT replay correctly rejects late sources, while M12/M13 are explicitly deferred |
| `reports/provenance_indexes/source_index.csv`, `observation_index.csv` | Manifest-resolvable source-run and exact-station observation identities with availability clocks, raw paths, and hashes | generated by `build_provenance_indexes.py`; the promoted scope is KNYC/KLAX/KAUS; nearby rows are explicitly scoped out, and rows missing any required provenance field are rejected without synthetic IDs or timestamps |
| Candle execution scenario output | prior closed signal, next-period fill scenario, YES/NO edge, fees, payoff, and summary risk metrics | generated by `simulate_candle_execution.py`; conservative/base/optimistic scenarios remain research-only and do not imply queue-aware fills |
| Paper-trading runner | append-only JSONL hypothetical order/fill records keyed to a validated prediction manifest | `run_paper_trading.py`; bounded iterations or continuous mode; no broker/exchange side effects |
| `reports/deployment_gate.json` | Per-prerequisite deployment evidence, failures, and explicit non-authorization flag | generated by `deployment_gate.py`; requires a supplied evidence snapshot and never authorizes order submission |
| `reports/forecast_completeness.json` | Per-model required-variable sets, observed fields, missing fields, row counts, and completeness status | generated by `audit_forecast_completeness.py`; current GEFS and NBM vectors are complete for their configured fields; HRRR retains surface pressure/radiation and derived wind speed while unsupported pressure-level/CAPE fields remain explicit; the current LAMP bulletin is aviation-only and lacks the requested temperature fields |
| `reports/settlement_target_audit.json` | Event-level source counts, target/rules-hash completeness, station-resolution method counts, and separate official HOMR coordinate candidates | generated by `audit_settlement_targets.py`; current 15,883 contracts have source/target/hash fields and a KNYC HOMR candidate (40.77898, -73.96925), but TWC-to-coordinate matching remains explicitly unverified |
| `kalshi_hourly/historical_quotes/market_metadata.csv` | Per-bounded-run historical REST market identity, open/close/settlement windows, result, source fields, and receipt clock | generated by `collect_historical_nyc_quotes.py` v3; accompanied by immutable paginated `/historical/markets` payloads in `raw_manifest.jsonl`; not promoted into the normalized contract table until event-level terms and station provenance reconcile |
| SQLite `historical_quote_market_metadata`, `historical_quote_candles`, `historical_quote_trades` | Query-layer copies of the immutable bounded REST quote archives, keyed by `series_ticker` plus market/trade identity | loaded by `load_sqlite.py`; raw JSON files and `raw_manifest.jsonl` remain authoritative; these tables contain aggregates/trades, not reconstructed historical L2 |
| `reports/settlement_source_transition.json` | Hash of the Kalshi source-transition notice, effective UTC cutoff, pre/post row counts, expected source by era, and mismatch list | generated by `audit_settlement_source_transition.py`; current archive has 15,883 pre-transition TWC rows and no post-transition Synoptic rows |
| `kalshi_hourly/terms/manifest.json` | Receipt-stamped hashes and local paths for the Kalshi contract and contract-terms PDFs referenced by the series metadata | generated by `collect_contract_terms.py`; both referenced PDFs are archived without claiming that their text supplies an exact settlement coordinate |
| `reports/nyc_edge_first_dataset.csv` / `nyc_edge_first_rejections.csv` | Strict admitted NYC PIT feature/label rows and one-reason-per-market rejection ledger | generated by `build_nyc_edge_dataset.py`; only feature rows with valid clocks, settled labels, and verified station mapping may enter the admitted file |
| `reports/nyc_edge_first_dataset_status.json` | Admission count, rejection count/reasons, and fail-closed pass status for the edge-first dataset | consumed by the `edge_first_nyc_dataset` quality gate; current status is red with zero admitted rows |
| `reports/edge_first_evaluation.json` | Fail-closed OOS evaluation status and metrics placeholder for admitted NYC PIT rows | generated by `evaluate_edge_first.py`; current status is `not_evaluable` with `metrics: null` because the admitted dataset is empty |
| `reports/nyc_edge_baseline_predictions.csv` / `nyc_edge_baseline_status.json` | PIT-only persistence baseline predictions, manifest count, and rejection status | generated by `generate_edge_baseline_predictions.py`; current run emits zero predictions and zero manifests because no admitted rows exist |
| `kalshi_hourly/active_probe/latest.json` | Receipt/hash and count for the latest public open-market probe | generated by `probe_active_nyc_market.py`; current count is zero and no contracts are synthesized |
| `settlement_locations.json` | User-supplied city/product/station/site coordinate registry with source and display-coordinate caveats | KNYC maps to Central Park (40.77900,-73.96925); consumed by settlement-target audits and contract normalization |
| `reports/training_readiness.json` | Chronological train/calibration/test folds, embargo, target-date coverage, and fail-closed readiness status | generated by `training_readiness.py`; current status is `data_missing` because no PIT decision-feature rows have parseable target dates |

The implemented state builder precomputes one row per observation with its own
`feature_asof_utc`: all running values use records at or before that exact
timestamp. The raw METAR parser exposes
precise `T` temperatures and standard 1/2/4 extrema remarks for a more
faithful official-extreme reconstruction.

`scripts/weather/settlement_semantics.py` is the single source of truth for
the P0 local-standard-time window and published whole-degree bucket geometry.
For Las Vegas, its daily NWS window is anchored to PST (UTC-8) all year: this
is 01:00 PDT through 00:59 PDT on the following civil date during daylight
saving time. Do not substitute a civil-midnight window.
# Forecast provenance

`forecasts/manifest.csv` columns: `model`, `city`,
`initialization_time_utc`, `valid_time_utc`, `lead_hours`, station latitude
and longitude, `request_url`, `raw_path`, `sha256`, and `ingested_at_utc`.
The corresponding filtered GRIB2 is retained under `forecasts/raw/{model}`.
`pit.available_asof` filters prediction features on both issue
(`initialization_time_utc`) and receipt/ingest (`source_receipt_time` or
`ingested_at_utc`); missing either timestamp fails closed. Use
`known_forecasts` for prediction features and `valid_forecasts_asof` only
when joining fields whose valid time must already have occurred.
