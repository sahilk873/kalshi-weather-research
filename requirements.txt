# Needed only to run the authenticated live WebSocket collector.
websocket-client>=1.8,<2
cryptography>=42,<47
# Optional forecast decoding (raw GRIB2 remains usable without these):
# eccodes>=2.38
# cfgrib>=0.9
# ECMWF open-data collection/decoding (credential-free forecast client):
# ecmwf-opendata>=0.3
# Copernicus ERA5 retrieval (requires CDS_API_KEY and account setup):
# cdsapi>=0.7
